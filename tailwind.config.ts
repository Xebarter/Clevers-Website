import type { Config } from "tailwindcss";

export default {
    darkMode: ["class"],
    content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))'
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))'
        },
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))'
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))'
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))'
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))'
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))'
        },
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        chart: {
          '1': 'hsl(var(--chart-1))',
          '2': 'hsl(var(--chart-2))',
          '3': 'hsl(var(--chart-3))',
          '4': 'hsl(var(--chart-4))',
          '5': 'hsl(var(--chart-5))'
        },
        // Kindergarten-friendly colors
        'kinder-red': '#FF5757',    // Bright red
        'kinder-blue': '#5DADE2',   // Sky blue
        'kinder-green': '#66CC99',  // Mint green
        'kinder-yellow': '#FFDD59', // Cheerful yellow
        'kinder-purple': '#AC8CD4', // Soft purple
        'kinder-orange': '#FF9966', // Warm orange
        'kinder-pink': '#FF99CC',   // Soft pink
      },
      fontFamily: {
        // Custom font variables
        'heading': 'var(--font-varela-round)',
        'body': 'var(--font-quicksand)',
        'accent': 'var(--font-comic-neue)',
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
        'xl': '1rem',
        '2xl': '1.5rem',
        '3xl': '2rem',
      },
      container: {
      center: true,
      padding: {
        DEFAULT: '1rem',
        sm: '2rem',
        lg: '4rem',
        xl: '5rem',
        '2xl': '6rem',
      },
      screens: {
        sm: '640px',
        md: '768px',
        lg: '1024px',
        xl: '1280px',
        '2xl': '1536px',
      },
      },
      boxShadow: {
        'kinder': '0 4px 6px rgba(0, 0, 0, 0.1), 0 0 0 6px rgba(255, 217, 0, 0.2)',
        'kinder-hover': '0 8px 15px rgba(0, 0, 0, 0.1), 0 0 0 8px rgba(255, 217, 0, 0.3)',
      },
    }
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
